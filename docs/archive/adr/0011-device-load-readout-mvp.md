# MVP Operator Preview shows Device Load Readout without throttling

Operator Preview includes a lightweight Device Load Readout (thermal status + approximate memory; cheap CPU/GPU if available) so operators can judge device stress on long tripod runs and when using Max-Sensor. This is display-only in MVP — it does not reintroduce ThermalGuard / automatic backoff (see ADR 0008).
