# v0.1.7 — ลำดับงานที่ต้องทำ

**เอกสารนี้คือ "ทำอะไร ตามลำดับไหน เสร็จเมื่อไร"**
**เหตุผลเบื้องหลังทุกข้ออยู่ที่ [V_0_1_7_PLAN.md](./V_0_1_7_PLAN.md)** — ที่นี่ไม่อธิบายซ้ำ อ้างเป็น §

**สถานะ:** ตัดสินครบ 9 ข้อ · ไม่มีอะไรบล็อก · **เริ่มที่ Slice 0 ได้ทันที**

> `S` **ย่อจาก** `Slice` **= ก้อนงานหนึ่งชิ้น** · `S0` คือชิ้นที่ศูนย์ (งานวัดก่อนเริ่ม) · ตัวย่ออื่นอยู่ในตารางถัดไป

---

## ตัวย่อในเอกสารนี้ — อ่านตารางนี้ก่อน


| ตัวย่อ                    | ย่อจาก                                 | หมายถึง                                                                                                                                                          |
| ------------------------- | -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **S0 · S1 · … · S11**     | **S** = **Slice** (ก้อนงานหนึ่งชิ้น)   | หน่วยงานหนึ่งชิ้นที่ทำจบแล้วตรวจได้ · **เป็นหน่วยเดียวกับ "Phase" ที่รีโปนี้ใช้อยู่** (เช่น B1, P9) — ลงทะเบียนไว้ที่ [CONVENTIONS.md §2](./CONVENTIONS.md) แล้ว |
| **PRE-1 · PRE-2 · PRE-3** | **PRE** = **Prerequisite** (งานเตรียม) | งานที่ไม่ใช่โค้ดฟีเจอร์ แต่ต้องเสร็จก่อน Slice บางตัวจะทำได้                                                                                                     |
| **W1 · W2**               | **W** = **Worker**                     | ขั้นตอนที่ทำงานเบื้องหลัง · **W1** = ตัวที่แตะภาพจริง · **W2** = ตัวที่อ่านแต่ข้อมูล ไม่แตะภาพ                                                                   |
| **D1 · D2**               | **D** = **Decision** (การตัดสินใจ)     | คำถามที่ต้องตัดสินก่อนลงมือ — **ตัดสินครบแล้วทั้งสองข้อ** เหตุผลอยู่ใน [PLAN](./V_0_1_7_PLAN.md)                                                                 |
| **ก้อน 1 – ก้อน 4**       | —                                      | กลุ่มของ Slice ที่ **ส่งงานได้เป็นชุด** ไม่ต้องรอชุดถัดไป                                                                                                        |
| **§**                     | Section                                | หมายถึงหัวข้อในเอกสาร เช่น "PLAN §3.7" = หัวข้อ 3.7 ของไฟล์ PLAN                                                                                                 |
| **ROI**                   | **R**egion **o**f **I**nterest         | สี่เหลี่ยมเล็ก ๆ ในภาพที่เราสนใจจะวัด เช่น กรอบใบหน้าคนหนึ่งคน                                                                                                   |


---



## คำที่ใช้บ่อยในเอกสารนี้

อ่าน 6 คำนี้ก่อน แล้วที่เหลือจะไม่สะดุด · ศัพท์อื่น ๆ อยู่ที่ [GLOSSARY_TH.md](./GLOSSARY_TH.md)


| คำ               | หมายถึง                                                                                                                                                         |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **chunk**        | ไฟล์วิดีโอสั้น ๆ ที่แอปอัดทีละไฟล์ · ที่ 4K ยาวประมาณ **10–12 วินาที** ต่อไฟล์ · แอปไม่อัดยาวไฟล์เดียวเพราะถ้าแอปดับกลางทาง ไฟล์ที่ยังไม่ปิดจะ**เปิดไม่ได้เลย** |
| **track**        | "หนึ่งครั้งที่ระบบมองเห็นคนหนึ่งคนต่อเนื่องกัน" · ยังไม่ใช่ "หนึ่งคน" เพราะคนเดียวที่เดินผ่านหลายรอบ = หลาย track                                               |
| **person ID**    | เลขประจำตัวที่ระบบแปะให้แต่ละคน เพื่อรู้ว่ากล่องในเฟรมนี้กับเฟรมก่อนเป็นคนเดียวกัน                                                                              |
| **frame / เฟรม** | ภาพนิ่งหนึ่งภาพจากวิดีโอ · ระบบไม่ได้ดูทุกเฟรม แต่**สุ่มดูทุก 120 มิลลิวินาที** (ประมาณ 8 ภาพต่อวินาที)                                                         |
| **candidate**    | รูปที่ระบบเซฟไว้ก่อนแบบ "อาจจะเก็บ" · สุดท้ายเลือกเก็บแค่ 3 รูปต่อคน ที่เหลือลบ                                                                                 |
| **baseline**     | ตัวเลข "ก่อนแก้" ที่เก็บไว้เทียบกับ "หลังแก้" · ไม่มี baseline = แก้แล้วพิสูจน์ไม่ได้ว่าดีขึ้น                                                                  |


---



## เทียบกับ [proposal_v0.1.7.md](./proposal_v0.1.7.md) §3

**เลข S0–S11 และการแบ่ง 4 ก้อน ตรงกัน 1:1** · ต่างกัน 5 จุด ทั้งหมดเป็นการเบี่ยงโดยเจตนา เหตุผลอยู่ [PLAN §12](./V_0_1_7_PLAN.md)


| S   | proposal เสนอ                                     | เอกสารนี้ทำ                                                                                                                                       | ทำไม                                                                           |
| --- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| S2  | จำกัด candidate ค้างไม่เกิน 150 ไฟล์ แล้ว**ทิ้ง** | **backpressure — รอ ไม่ทิ้ง** · เพดานเป็นไบต์ + chunk ที่ค้าง                                                                                     | ทิ้ง candidate = ทิ้งนักวิ่ง · pipeline offline ไม่มี deadline (PLAN §12 R1.1) |
| S3  | `fd.sync()` ก่อน handoff                          | **ปิด stream ก่อน enqueue**                                                                                                                       | W1/W2 โปรเซสเดียวกัน · `fsync` กันไฟดับ ไม่ได้กัน partial read (R3.1)          |
| S8  | เน้นวัดความคมชัดที่ Face ROI                      | **ทำไปแล้วตั้งแต่ v0.1.6** ([VideoFrameProcessor.kt:712](../androidApp/src/main/kotlin/com/autobots/camera/pipeline/VideoFrameProcessor.kt#L712)) | แต่เป็นที่มาของ D1 — คนไม่มีหน้าถูกวัดคนละสเกล (R4.2)                          |
| S8  | ฝัง `personId` ใน Exif ตอน W1                     | **2 จังหวะ** — W1 ฝัง `track[]` · S9 เขียนทับเพิ่ม `personId[]`                                                                                   | W1 ยังไม่รู้จัก `personId` (เกิดตอน S9) · รูปเป็นเฟรมเต็มมีหลายคน (R5)         |
| S11 | รัน 60–90 นาที                                    | **รัน 3–4 ชั่วโมง**                                                                                                                               | ตรงตามเป้าที่ proposal เขียนไว้เองใน Executive Summary                         |


**เพิ่มจาก proposal:** Hungarian ใน S6 (D2) — ID switch เป็นความผิดเดียวในแผนที่ไม่มีตัวจับ

---



## ภาพรวม

```
📦 ก้อน 1  S1 → S2 → S3 → S4   บันทึกและหลักฐาน      ความเสี่ยงต่ำ · คุ้มแน่
        ▼                        ← ส่งได้ตรงนี้ ใช้งานได้จริง
📦 ก้อน 2  S5 → S6 → S7 → S8   เปลี่ยนแกนเป็นคน       ความเสี่ยงกลาง
        ▼                        ← ส่งได้ตรงนี้
📦 ก้อน 3  S9 → S10             เย็บรอยต่อ + เอกสาร
        ▼
📦 ก้อน 4  S11                  ทดสอบ 3–4 ชม.
```

**แต่ละก้อนส่งได้เอง** ไม่ต้องรอก้อนถัดไป — ก้อน 1 มีประโยชน์ทันทีแม้ไม่ทำก้อน 2 เลย

---



## 🔧 เตรียมล่วงหน้า — 3 อย่างที่ไม่ใช่โค้ดฟีเจอร์

ทำคู่ขนานไปกับก้อน 1 ได้ **แต่ต้องเสร็จก่อนที่ระบุ**

- [ ] **สร้าง test source set** — ต้องเสร็จก่อน **S6**
  `shared/build.gradle.kts` มี `commonTest.dependencies { }` **ว่างเปล่า** และ **ไม่มีโฟลเดอร์ test เลยในโปรเจกต์**
  ⇒ เพิ่ม `implementation(kotlin("test"))` + สร้าง `shared/src/commonTest/kotlin/com/autobots/camera/`
  ⇒ ยืนยันด้วย `./gradlew :shared:allTests` ให้รันผ่านด้วย test เปล่า ๆ ก่อน
- [ ] **หาคลิปยาว 3–4 ชม.** — ต้องมีก่อน **S11**
  ถ้าไม่มี ให้เตรียมแผนต่อคลิป 93 นาทีหลายรอบในหนึ่ง session แทน (ได้ความร้อน + จำนวนไฟล์ครบ)
- [ ] **ทำ "เฉลย" ด้วยมือ จากคลิปสั้น 1–2 นาที** — ต้องมีก่อนตรวจผลของ **S6**
  เฉลย = คนนั่งดูคลิปแล้วบอกเองว่า *"ในภาพนี้ คนที่ยืนซ้ายคือคนหมายเลข 1 คนขวาคือหมายเลข 2"* ทุกภาพ
  ⇒ ใช้เทียบว่าระบบ**สลับตัวคน**ไปกี่ครั้ง (เครื่องมือเรียกค่านี้ว่า `IDSW`)
  ⚠️ **ไม่มีเฉลย = วัดไม่ได้เลย** และ **เป็นงานที่คนต้องนั่งทำ ไม่ใช่แค่ติดตั้งโปรแกรม** (PLAN §13)

---



## 📏 S0 · วัดของเดิม — ทำหลังก้อน 1 (ไม่ใช่ก่อน)



### S0 คืออะไร

**คือการเอาไฟล์ที่แอปเขียนไว้มานับ 3 ตัวเลข** เพื่อตัดสินว่าก้อน 2 กับก้อน 3 คุ้มทำไหม

> ✅ **S0.2 / S0.3 วัดแล้ว 2026-08-28** → [reports/v0.1.7/s0_baseline.md](../reports/v0.1.7/s0_baseline.md)
> `S0.3 = 34.3%` ⇒ **ก้อน 2 คุ้มทำ ยืนยันแล้ว** · `S0.2 = 4.2%` ⇒ **ก้อน 3 ลดความสำคัญ**
> `S0.1` ยังวัดไม่ได้ — ต้องรอ S4 พิมพ์ `recordedAtEpochMs` แล้วอัด live

> ⚠️ **ทำก่อนก้อน 1 ไม่ได้** — ต้องมี `tracks.csv` จาก session ที่อัด **live** และวันนี้ยังไม่มีสักไฟล์
> และ `session_log.txt` **ไม่ได้พิมพ์** `recordedAtEpochMs` ⇒ S0.1 ต้องรอ S4 เพิ่มบรรทัดนั้นก่อน
> ⇒ **ลำดับจริง: ทำก้อน 1 → อัด live สัก 3–5 นาที → ค่อยรัน S0 → แล้วตัดสินก้อน 2/3**

ทำเพื่อ 2 อย่าง:

1. **เก็บตัวเลข "ก่อนแก้" ไว้เทียบ** — ถ้าไม่มี พอแก้เสร็จก็เถียงกันไม่จบว่าดีขึ้นจริงไหม
2. **เช็กว่างานบางอย่างในแผน "ทำได้ไหม"** — มีงานหนึ่ง (S9) ที่ถ้าตัวเลขออกมาไม่ดี **จะทำไม่ได้เลยไม่ว่าเขียนโค้ดเก่งแค่ไหน** รู้ตอนนี้ถูกกว่ารู้ตอนเขียนเสร็จแล้ว

---



### ⛔ S0.1 · "ตอนแอปสลับไฟล์ วิดีโอขาดไปนานเท่าไร"

**เรื่องมันเป็นแบบนี้:** แอปไม่ได้อัดวิดีโอยาวไฟล์เดียว มันอัดเป็นไฟล์สั้น ๆ ไฟล์ละ 10–12 วินาที แล้วสลับไฟล์ใหม่เรื่อย ๆ
**ตอนสลับไฟล์ จะมีช่วงสั้น ๆ ที่ไม่ได้อัดอะไรเลย** — เราต้องรู้ว่ามันนานแค่ไหน

**ทำไมต้องรู้:** ถ้านักวิ่งกำลังวิ่งผ่านอยู่ตอนที่แอปสลับไฟล์ ระบบจะเห็นเขาหายไปแวบหนึ่งแล้วโผล่มาใหม่ ⇒ ต้องเดาว่า "คนที่โผล่มา คือคนเดิมไหม"


| ขาดไปนานเท่าไร           | เดาได้ไหม                                               |
| ------------------------ | ------------------------------------------------------- |
| **น้อยกว่า 0.12 วินาที** | ✅ เหมือนหายไปแค่ภาพเดียว — เดาง่าย ทำ S9 ได้เลย         |
| **0.12–0.6 วินาที**      | ⚠️ ยังเดาได้ แต่ต้องระวังการตั้งค่า                     |
| **มากกว่า 0.6 วินาที**   | ⛔ **คนขยับไปไกลเกินจะเดาได้** — **ยกเลิก S9 ทั้ง task** |


**วิธีนับ** — เปิด `session_log.txt` แล้วเอาเวลาที่ไฟล์หนึ่งจบ (`recordedAtEpochMs` + `recordDurationMs`) ไปลบกับเวลาที่ไฟล์ถัดไปเริ่ม (`recordedAtEpochMs`)

---



### ✅ S0.2 · "ตอนนี้มีนักวิ่งกี่คน ที่ถูกนับเป็นสองคนเพราะวิ่งคร่อมรอยต่อไฟล์"

**ปัญหาวันนี้:** ระบบเริ่มนับคนใหม่หมดทุกครั้งที่สลับไฟล์ ⇒ นักวิ่งที่วิ่งคร่อมรอยต่อพอดี **กลายเป็นสองคน** ⇒ ได้รูป 6 ใบแทน 3 ใบ และตัวเลขนับคนก็เกินจริง

**ต้องรู้เพื่อ:** เป็นตัวเลข "ก่อนแก้" ที่ S9 ต้องทำให้ลดลง

**วิธีนับ** — เปิด `tracks.csv` หาคนที่**เห็นครั้งสุดท้ายติดท้ายไฟล์พอดี** (`lastUs` ห่างจากท้ายไฟล์ไม่ถึง 120 ms) แล้วดูว่าในไฟล์ถัดไปมีคนที่**เห็นครั้งแรกติดต้นไฟล์พอดี** (`firstUs` ไม่ถึง 120 ms) ที่น่าจะเป็นคนเดียวกันไหม

---



### ✅ S0.3 · "ตอนนี้มีนักวิ่งกี่คน ที่ระบบเห็นแค่ภาพเดียวแล้วหลุด"

**สิ่งที่ควรเกิด:** ระบบตามคนหนึ่งคนได้ต่อเนื่องหลายภาพ ⇒ ตอนเลือกรูปจะมีตัวเลือกหลายใบ
**สิ่งที่กลัว:** ระบบตามไม่ติด เห็นภาพเดียวก็หลุด แล้วนับเป็นคนใหม่ ⇒ เต็มไปด้วย "คน" ที่ถูกเห็นแค่ครั้งเดียว

**ต้องรู้เพื่อ — ข้อนี้ตัดสินว่าจะทำก้อน 2 หรือเปล่า:**


| ผลออกมา          | แปลว่า                                                                                           |
| ---------------- | ------------------------------------------------------------------------------------------------ |
| **เยอะ**         | ระบบตามคนไม่ติดจริง ⇒ **ก้อน 2 คุ้มทำ**                                                          |
| **น้อยอยู่แล้ว** | ระบบตามคนได้ดีอยู่แล้ว ⇒ **ทบทวนว่าก้อน 2 คุ้มไหม อาจทำแค่ก้อน 1 ก็พอ** — ประหยัดงานไปครึ่งหนึ่ง |


**วิธีนับ** — เปิด `tracks.csv` นับแถวที่คอลัมน์ `frames` เท่ากับ 1 แล้วหารด้วยจำนวนแถวทั้งหมด

---



### สิ่งที่ต้องส่งมอบจาก S0

- [ ] นับทั้ง 3 ตัวเลขจากคลิปที่วัดไว้แล้ว — `run4mins` และคลิปเส้นชัยกลางคืน (ถ้ายังเก็บไฟล์ไว้)
- [ ] เขียนผลลง `reports/v0.1.7/s0_baseline.md` ตามแบบเดียวกับ `reports/v0.1.3/`
- [ ] **ตอบให้ได้ 2 ข้อ ก่อนเริ่มเขียนโค้ด:** ทำ S9 ไหม (จาก S0.1) · ทำก้อน 2 ไหม (จาก S0.3)
- [ ] อัปเดตคำตอบนั้นลง [V_0_1_7_PLAN.md](./V_0_1_7_PLAN.md) §7

> ⛔ **ห้ามข้าม S0.1** — ถ้าวิดีโอขาดเกิน 0.6 วินาที งานทั้งก้อน 3 ทำไม่ได้ **การรู้ตอนนี้ประหยัดงานไปทั้งก้อน**

---



# 📦 ก้อน 1 — บันทึกและหลักฐาน

**เป้าของก้อน:** ข้อมูล tracking หยุดหายไปกับหน่วยความจำ · crash แล้วยังได้ข้อมูลถึง chunk ล่าสุด
**ยังไม่แตะ tracker และยังไม่เปลี่ยนแกน** — จำนวนรูปที่เก็บได้ต้องเท่าเดิมทุก slice

## ✅ S1 · `score` เดินทางครบสาย — **เสร็จแล้ว** (`0b94ff3`)

> เล็กที่สุด และปลดล็อกทุก slice ที่เหลือ

**แตะ**

- `shared/src/commonMain/kotlin/com/autobots/camera/SubjectTracker.kt` — `TrackedBox` เพิ่ม `score: Float = 1f`
- `androidApp/src/main/kotlin/com/autobots/camera/pipeline/VideoFrameProcessor.kt` — บรรทัด **613** `observed = inZone.map { it.bounds }` · `Sighting` · `recordSighting()`

**ทำ**

- [x] `TrackedBox` รับ `score` (default `1f` ให้ path face/pose ยังคอมไพล์ผ่าน)
- [x] `Sighting.boxes` ถือ `DetectedPerson` แทน `Rect` เปล่า
- [x] `recordSighting()` ส่ง score ต่อ

**เสร็จเมื่อ**

- [x] **ไม่มีค่า default ปลอม** — `score` เป็น `Float?` · null = detector ไม่รายงาน (ลอกแบบแผน `FrameQuality.Score.confidence`)
- [x] คอมไพล์ผ่าน (`:androidApp:compileDebugKotlin`)
- [x] **จำนวนรูปไม่เปลี่ยน — ยืนยันบนเครื่องจริง 2026-08-31** (`24069PC21G`)
  A/B บน `run4mins.mp4` · Face + Person · NPU · Whole frame — บิลด์ก่อน S1 vs หลัง S1
  `tracks.csv` และ `photos.csv` **เหมือนกันทุกแถว** (98 / 110 แถว) · `passages=97 captured=35 chunks=23` เท่ากันทั้งสองรอบ



## S2 · ไฟล์ต่อ chunk + streaming + guardrail ดิสก์

> PLAN §3.6 · §3.7

**แตะ**

- `androidApp/src/main/kotlin/com/autobots/camera/pipeline/CapturePipelineCoordinator.kt` — `trackRows` / `photoRows` (บรรทัด ~215) · `writeTrackIndex()` · `writePhotoIndex()` · `writeSessionFile()` · `hasStorageForRecording()` (~711)
- **ไฟล์ใหม่:** ตัวเขียน CSV แบบ streaming

**ทำ**

- [ ] เลิกสะสมแถวในหน่วยความจำ — เขียน `sightings/c000N.csv` + `tracks/c000N.csv` จบต่อ chunk แล้วปิดไฟล์
- [ ] `chunks.csv` append ต่อ chunk (คอลัมน์ตาม PLAN §3.4)
- [ ] ไฟล์รวมตอนจบเขียนด้วย `BufferedWriter` ไล่แถว — **ไม่ใช่** `buildString`
- [ ] **startup orphan cleanup** — ล้าง `staging/` `sightings/` `tracks/` ที่ตกค้าง ทุกครั้งที่เปิดแอป
- [ ] **disk watermark ระหว่างรัน** — เหลือ < 2 GB หรือ < 5% ⇒ บังคับ flush ก่อนรับเพิ่ม
- [ ] แบ่ง subdirectory เผื่อ ~2,410 ไฟล์ที่ 3.5 ชม. (PLAN §3.8)

**เสร็จเมื่อ**

- [ ] ฆ่าโปรเซสกลางคัน ⇒ **มีข้อมูลถึง chunk ล่าสุด** ไม่ใช่ศูนย์
- [ ] เปิดแอปใหม่หลังฆ่า ⇒ **ไม่มีไฟล์ตกค้าง**
- [ ] **peak heap ไม่โตตามความยาว session**
- [ ] `sourceOffsetUs` — ⚠️ **ไปตรวจที่ splitter ว่ารู้ค่านี้ไหม** ถ้าไม่รู้ ให้บันทึกว่าเป็นค่าประมาณ (PLAN §3.4)



## S3 · แยก Worker 1 / Worker 2

> PLAN §4 · รอยต่ออยู่**หลัง** sharpness

**แตะ**

- `CapturePipelineCoordinator.kt` — consumer ที่บรรทัด **245** (`for (item in videoQueue)`) · เพิ่มคิวที่สอง
- `VideoFrameProcessor.kt` — `selectKeepers()` ย้ายออกไปเป็น W2

**ทำ**

- [ ] W1 = decode + detect + tracker + sharpness → เขียน candidate ลง staging + csv → **ปิด stream แล้วค่อย enqueue**
- [ ] W2 = อ่าน csv ของ chunk ตัวเอง → เลือก 3/คน → ลบตัวแพ้ → ส่งคิว upload
- [ ] **backpressure ที่ staging** — เต็มแล้ว W1 **รอ ไม่ใช่ทิ้ง** · เพดานเป็น **ไบต์ + จำนวน chunk ที่ค้าง**

**เสร็จเมื่อ**

- [ ] จำนวนรูปที่เก็บได้ **ไม่เปลี่ยนเลย**
- [ ] หยุด W2 ไว้ ⇒ staging **หยุดโตที่เพดาน** ไม่ใช่โตต่อ
- [ ] `realtimeRatio` สังเกตไว้ ไม่แตะ 0.85 (PLAN §8.1)



## S4 · `tracks.csv` คอลัมน์ใหม่

**แตะ**

- `shared/src/commonMain/kotlin/com/autobots/camera/TrackSummary.kt`
- `CapturePipelineCoordinator.writeTrackIndex()`

**ทำ**

- [ ] `enteredMs` `exitedMs` `visibleMs` `sessionEnteredMs`
- [ ] `frames` → `framesSeen` + `framesSpan` `framesMissed` `trackedRatio`
- [ ] `speedMps` (สูตร PLAN §4.3) + `meanScore` `lowTierFrames`
- [ ] บรรทัด `#` เตือน: **การเปลี่ยนชื่อ** `frames` · ความไม่แน่นอน **±120 ms** · สมมติฐานของ `speedMps`
- [ ] `sampleIntervalMs` ส่งเข้า `TrackSummary` **ไม่ hardcode 120**
- [ ] **พิมพ์** `recordedAtEpochMs` **ต่อ chunk ลง** `session_log.txt` (`PipelineSessionRecord.kt:296` บล็อก Chunk) — 1 บรรทัด · **S0.1 คำนวณไม่ได้ถ้าไม่มี**

**เสร็จเมื่อ**

- [ ] ตัวเลขบนจอ Session history กับในไฟล์ **ตรงกัน**
- [ ] สคริปต์เก่าที่อ่าน `frames` **พังแบบเห็นชัด** ไม่ใช่อ่านค่าผิดเงียบ ๆ

> ✅ **ส่งก้อน 1 ได้ตรงนี้** — ได้บันทึกที่อยู่รอด + `framesMissed` + ข้อมูลสำหรับ overlay บนคอม

---



# 📦 ก้อน 2 — เปลี่ยนแกนเป็นคน

**เป้าของก้อน:** คนที่วิ่งคู่กับคนตัวใหญ่กว่าต้องได้รูปของตัวเอง

## S5 · detector คืนสองชั้น

**แตะ** `androidApp/src/main/kotlin/com/autobots/camera/detection/PersonFootDetector.kt` (~206)

**ทำ**

- [ ] `lowScoreFloor` = **absolute 0.1 ตาม ByteTrack** (ไม่ใช่ `minScore/2`)
- [ ] คืนพร้อมธงบอกชั้น (`high` / `low`)
- [ ] **เฉพาะ tracker เห็นชั้นล่าง** — gate เลือกรูปใช้ชั้นบนเดิม

**เสร็จเมื่อ**

- [ ] จำนวนรูปที่เก็บได้ **ไม่เปลี่ยนเลย** ⇒ ถ้าเปลี่ยน = **ชั้นล่างรั่วเข้า gate**



## S6 · ByteTrack + DIoU + Hungarian

> PLAN §5 · **ต้องมี test source set ก่อน** (ดู "เตรียมล่วงหน้า")

**แตะ**

- `shared/src/commonMain/kotlin/com/autobots/camera/SubjectTracker.kt`
- **ไฟล์ใหม่:** `Hungarian.kt` (commonMain) · test ใน `shared/src/commonTest/`

**ทำ**

- [ ] แบ่งชั้น high `> 0.5` / low `0.1–0.5`
- [ ] สามชั้นตาม PLAN §5.1 · `lost` + `re_activate` · `unconfirmed` · `fuse_score`
- [ ] **metric = DIoU** แทน IoU (PLAN §5.3)
- [ ] **Hungarian แทน greedy** (D2) — ~150 บรรทัด ไม่มี dependency
- [ ] **height penalty** จาก hard gate (`MAX_SIZE_RATIO`) → พจน์ถ่วงต่อเนื่อง
- [ ] **direction consistency** — กลับลำเกิน ~90° ใน 120 ms ⇒ ไม่ match ปล่อยเป็น `lost` (**ไม่ใช่ลดชั้นเป็น unconfirmed**)
- [ ] ค่าคงที่ตาม fps จริง 8.33 — `max_time_lost` · **process noise ของ Kalman คูณตาม dt** · `match_thresh` จูนใหม่
- [ ] เก็บ `matchedLowTier` ต่อ track
- [ ] ❌ **ไม่ทำ Kalman เต็มรูป** — เลื่อน

**เสร็จเมื่อ**

- [ ] **Hungarian = brute force 10,000/10,000 เมทริกซ์ที่** `N ≤ 6` ← ปิดความเสี่ยง "solver เขียนผิด"
- [ ] **S0(ค) ลดลง** — track ที่ `framesSeen == 1` น้อยลง
- [ ] `IDSW` ไม่แย่ลง (ต้องมี GT)
- [ ] `framesMissed` รวม **ลดลง** — ⛔ **ถ้าไม่ลด ให้ถอย ไม่ใช่จูนต่อ**



## S7 · หน้า ↔ ตัว จับคู่ทุกคน

**แตะ**

- `VideoFrameProcessor.evaluateFrame()` — `val relevant = inZone.filter { it.bounds.contains(anchor...) }`
- `androidApp/src/main/kotlin/com/autobots/camera/detection/FaceDetLiteDetector.kt` — เลิกยุบเหลือกล่องใหญ่สุด

**ทำ**

- [ ] เอาหน้า**ทุกกล่อง**มาจับคู่กับตัว**ทุกกล่อง**
- [ ] ทุก person ได้ `faceScore` ของตัวเอง (ว่างได้ถ้าจับคู่ไม่ได้)

**เสร็จเมื่อ**

- [ ] เฟรมที่มีหลายคนและเห็นหน้าหลายหน้า ⇒ **แต่ละคนได้** `faceScore` **ของตัวเอง** ไม่ใช่ใช้ร่วมกัน



## S8 · quality ต่อคน

> PLAN §1.3–1.5 · **slice ที่ใหญ่ที่สุดของก้อนนี้**

**แตะ**

- `VideoFrameProcessor.evaluateFrame()` · `shared/.../FrameQuality.kt` · ตัวเขียน `sightings.csv` · `photos.csv`

**ทำ**

- [ ] gate ต่อคน — เฟรมไม่ถูก reject อีก **คนในเฟรมถูก reject**
- [ ] sharpness ต่อ ROI ของแต่ละคน
- [ ] **clamp ROI ด้วย** `Rect.intersect(0,0,w,h)` ก่อน crop — `isUsableRoi()` เช็คแค่ขนาด **ไม่ได้ clamp ขอบ**
- [ ] `FrameQuality` ต่อคน
- [ ] **D1**: `no_face` ⇒ **ไม่ได้รูป** แต่ **ยังให้คะแนน** (drop เทอม sharpness แล้วเกลี่ยน้ำหนัก — **ลอกแบบแผน** `confidence` **ในไฟล์เดียวกัน**)
- [ ] `sightings.csv` เต็มรูปตาม PLAN §3.1
- [ ] `photos.csv` เป็น **1 แถว = (file × personId)**
- [ ] **Exif จังหวะแรก** — W1 ฝัง `sessionId` + `chunk` + `track[]` (ยังไม่มี `personId`)
- [ ] ⚠️ **ตัดสิน privacy ก่อนฝัง Exif** — มันติดไปกับรูปที่ส่งลูกค้า

**เสร็จเมื่อ**

- [ ] **คนที่วิ่งคู่กับคนตัวใหญ่กว่าได้รูปของตัวเอง** ← เป้าหลักของ v0.1.7
- [ ] `no_face` มี `quality` แต่**ไม่มีรูป**
- [ ] ไม่มี `IllegalArgumentException` จาก ROI หลุดขอบ
- [ ] `realtimeRatio` สังเกตไว้ ไม่แตะ 0.85

> ✅ **ส่งก้อน 2 ได้ตรงนี้**

---



# 📦 ก้อน 3 — เย็บรอยต่อ + เอกสาร

> ⛔ **ทำเฉพาะเมื่อ S0(ก) วัดได้ gap ≤ 600 ms**



## S9 · เย็บรอยต่อ chunk

**แตะ** `CapturePipelineCoordinator.kt` — stage 3 ใหม่ ตอนจบ session

**ทำ**

- [ ] อ่าน `sightings.csv` ทุก chunk → จับคู่ที่รอยต่อด้วย **DIoU + extrapolate ด้วย velocity** (ฟังก์ชันเดียวกับ S6)
- [ ] ออก `personId` **เป็นคอลัมน์เพิ่ม ไม่เขียนทับ** `track`
- [ ] W2 **ค้าง candidate ของ person ที่แตะขอบ** ไว้ก่อน · โควตา 3 รูปคิดจาก `personId`
- [ ] **Exif จังหวะสอง** — เขียนทับเพิ่ม `personId[]`
- [ ] ครอบด้วย `withContext(NonCancellable)` **+ timeout**
- [ ] **การันตีลบ boundary candidate แม้ S9 ถูกข้าม** ← รูที่หาเจอยากที่สุด

**เสร็จเมื่อ**

- [ ] นักวิ่งที่ S0(ข) ระบุว่าถูกหั่น ⇒ ได้ `personId` เดียวกัน **และได้ 3 รูป ไม่ใช่ 6**
- [ ] กด Stop กลางคัน ⇒ csv รวมเขียนจบ **ไม่แครช**
- [ ] ไฟล์ตกค้าง **0 ไฟล์** ทุกกรณี



## S10 · เอกสาร

- [ ] [DOCS.md](./DOCS.md) · [ROADMAP.md](./ROADMAP.md) · [PIPELINE_FLOW.md](./PIPELINE_FLOW.md) · [SEQUENCE_FLOW.md](./SEQUENCE_FLOW.md)
- [ ] **[DESIGN_FLOW.md](./DESIGN_FLOW.md) — Video Pipeline 13 ขั้นเปลี่ยนลำดับ** (tracking ย้ายจากขั้นท้ายมาต้น)
- [ ] `reports/v0.1.7/report.md` ตาม [REPORT_GUIDELINE_TH.md](./REPORT_GUIDELINE_TH.md)
- [ ] `./scripts/check_docs_drift.sh` ผ่าน

---



# 📦 ก้อน 4 — ทดสอบระดับสนาม



## S11 · รัน 3–4 ชั่วโมง

> PLAN §3.8 — **ความเสี่ยงทั้งหมดของ §3.7 โผล่เฉพาะเมื่อรันนาน** คลิปสั้นจะผ่านทุกครั้งแล้วพังในสนาม

- [ ] รัน **3–4 ชม.** (หรือต่อคลิป 93 นาทีหลายรอบ)
- [ ] **peak heap ไม่โตตามเวลา**
- [ ] **ไฟล์ตกค้าง 0** — ทั้งจบปกติ · กด Stop · ถูกฆ่าโปรเซส (~2,410 ไฟล์ต้องกวาดครบ)
- [ ] `realtimeRatio` **คู่กับ** `driftPercent` ⇒ แยกความร้อนออกจากต้นทุนใหม่
- [ ] staging ไม่ทะลุเพดาน · backpressure ทำงานจริง
- [ ] `realtimeRatio` **≤ 0.85** ← ประตูเดียวของ perf

---



## 🛑 กฎการถอย — เขียนไว้ก่อนเริ่ม ไม่ใช่ตอนเจอปัญหา


| เจออะไร                                                | ทำอะไร                                                                                   |
| ------------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| **S0.1** — วิดีโอขาดเกิน **0.6 วินาที** ตอนสลับไฟล์    | **ยกเลิกก้อน 3 ทั้งก้อน** · ไปพิจารณาทางเลือก C (อัดไฟล์ให้ยาวขึ้น) — PLAN §11           |
| **S0.3** — ระบบตามคนติดดีอยู่แล้ว                      | **ทบทวนว่าก้อน 2 คุ้มไหม** · ทำแค่ก้อน 1 ก็ได้                                           |
| **S6 แล้ว** จำนวนภาพที่ระบบ*ควรเห็นแต่ไม่เห็น* ไม่ลดลง | **ถอย ไม่ใช่จูนต่อ** — แปลว่าตัวตรวจจับคนไม่ได้หลุดตั้งแต่แรก งานนี้แก้ปัญหาที่ไม่มีอยู่ |
| **S8 แล้ว** จำนวนรูปที่เก็บได้ **ลดลง**                | มีอะไรพัง — งานติดตามคนไม่ควรทำให้ได้รูปน้อยลง                                           |
| **ประมวลผลช้ากว่า 0.85 เท่าของความยาววิดีโอ**          | ดูตัวเลขความร้อน (`driftPercent`) ก่อน · ถ้าไม่ใช่ความร้อน ให้ทบทวนการวัดความคมชัดต่อคน  |
| **ที่พักรูปชั่วคราวบวมขึ้นเรื่อย ๆ** ทั้งที่มีตัวหยุด  | หยุดงาน · เพดานหรือการล้างไฟล์ไม่ทำงาน — **ห้ามแก้ด้วยการทิ้งรูป**                       |


---



## Related

- **เหตุผลทั้งหมด:** [V_0_1_7_PLAN.md](./V_0_1_7_PLAN.md) — §12 คือการตอบกลับรีวิว
- [DESIGN_FLOW.md](./DESIGN_FLOW.md) · [PIPELINE_FLOW.md](./PIPELINE_FLOW.md) · [CONVENTIONS.md](./CONVENTIONS.md) · [REPORT_GUIDELINE_TH.md](./REPORT_GUIDELINE_TH.md)

