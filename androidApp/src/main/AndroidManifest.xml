<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Autobots">
        <!-- Portrait only — avoids mid-session rotation tearing down capture. -->

        <!-- FastRPC, the transport the QNN HTP backend uses to reach the Hexagon NPU.
             These are vendor libraries listed in /vendor/etc/public.libraries.txt, and since
             API 30 an app's linker namespace hides any such library it has not declared. Without
             this the symptom is not a load error but a puzzling one two layers down:

                 QnnDsp <E> loadRemoteSymbols failed with err 4000
                 QnnDsp <E> Failed to load skel, error: 4000

             which TFLite then reports only as "Failed to apply delegate". Proved by running
             qnn-platform-validator under `run-as`: from the app's context the RPC libraries are
             "Missing", from a shell they load fine.

             required=false so the app still installs on devices without a Hexagon DSP; the NPU
             backend simply reports itself unavailable there. -->
        <uses-native-library android:name="libcdsprpc.so" android:required="false" />
        <uses-native-library android:name="libadsprpc.so" android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|uiMode"
            android:theme="@style/Theme.Autobots">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
